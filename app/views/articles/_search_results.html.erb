<% if !results.nil? and !results.empty? then %>
<table id="result_table" class="table table-hover span12" style="display: none;">
  <!-- <thead> -->
  <!--     <th class='span12'> -->
  <!-- 	<%= sortable "Title" %> -->
  <!--     </th> -->
  <!--     <th class='span4'> -->
  <!-- 	<%= sortable "Occurred" %> -->
  <!--     </th> -->
  <!-- </thead> -->
  <thead></thead>
  <tbody>
    <% for result in results %>
    <% if @article.nil? or result.id != @article.id then %>
    <tr>
      <td class='span12'><%= result.title %><%=hidden_field_tag 'result', result.current_content, class:'current_content'%><%=hidden_field_tag 'result', result.id, class:'article_id' %><%=hidden_field_tag 'result', result.occurred, class:'article_occurred'%><%=hidden_field_tag 'result', result.title, class:'article_title'%></td>
      <!-- <td><%= Time.zone.parse(calculate_date(result)).strftime "%m/%d/%y %I%p" %></td> -->
    </tr>
    <% end %>
    <% end %>
  </tbody>
</table>
  <%= will_paginate results, :renderer => BootstrapPagination::Rails, :inner_window => 0, :outer_window => 0, :page_links => false, :previous_label => "←", :next_label => "→", :params => { :controller => "articles", :action => "search_articles"} %>
<% end %>



